# ============================================================
# IndiaGoVisa.com — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your real values.
# NEVER commit .env.local to git.
# In Vercel: add these in Project → Settings → Environment Variables
# ============================================================

# --- SUPABASE (Frontend — Vite reads VITE_ prefix) ---
# Found in: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# --- SUPABASE (Backend / Serverless functions only) ---
# Same URL as above, without VITE_ prefix
SUPABASE_URL=https://your-project-id.supabase.co
# Service Role key — bypasses RLS. KEEP SECRET. Never expose to frontend.
# Found in: Supabase Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# --- STRIPE ---
# Found in: Stripe Dashboard → Developers → API Keys
STRIPE_SECRET_KEY=sk_live_...         # use sk_test_... for testing
# Found in: Stripe Dashboard → Developers → Webhooks → your endpoint → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# --- APP URL (used in Stripe redirect URLs) ---
# Your production domain, no trailing slash
APP_URL=https://www.indiagovisa.com
